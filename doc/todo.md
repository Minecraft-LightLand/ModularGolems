# Editor TODO

- Fix the issue that there is no Chinese site for home page.
- Automatic detect whether to display Chinese or not.
- Fix the issue that material page mod list occupying too much space. Make them collapse-able.
- fix the issue that item description in item list is too narrow for each item 
- In item page, group upgrades by compat mod
- Render blue arrow / purple arrow for upgrades that should have them.
- Add a version switcher, allowing switching between 1.19.2(branch 1.19)/1.19.4/1.20.1(branch 1.20)/1.21.1(branch 1.21)/26.1.2(branch 26.1). 
Hide items and materials if they don't exist in corresponding version. 
Note that patchouli contains entries that does not exist in current version. Use CompatManager of each branch to tell if a compat mod is supported for that version.